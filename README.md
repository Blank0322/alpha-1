# alpha-1

K-line strategy research scaffold (single-asset, event-driven backtest).

## Goal
Build a clean baseline framework for:
- indicator calculation
- signal generation
- position/risk rules
- backtest + report

## Quick start

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python scripts/run_demo.py
```
